SECRET_KEY = os.environ.get ('SECRET_KEY' , "b-70h9*cl87&p6qbnb7dj^v^jyr5^f#atu0ndxg0omu284r94j")
# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ .get ('DEBUG' , 'False') 
ALLOWED_HOSTS = os .environ.get ('ALLOWED_HOSTS' , 'localhost , 127.0.0.1').split (',')
DATABASES = {
    'default':  {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.environ.get('DB_NAME', 'BASE_H'),
        'USER': os.environ.get('DB_USER', 'postgres'),
        'PASSWORD': os.environ.get('DB_PASSWORD', 'Hawahawa05'),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get ('DB_PORT','5433'),
        #conn_max_age=600,
        #ssl_require=False  # Render utilise SSL automatiquement si nécessaire
    }
}

#!/usr/bin/env bash
set -o errexit
gunicorn monprojet.wsgi:application --bind 0.0.0.0:$PORT --workers 2
